services:
  - type: web
    name: devalert
    env: python
    buildCommand: "./build.sh"
    startCommand: "cd backend && gunicorn wsgi:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5

  - type: static
    name: devalert-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_URL
        value: "https://CHANGE_ME_TO_BACKEND_URL.onrender.com/api"
