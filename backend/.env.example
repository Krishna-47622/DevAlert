# ============================================================================
# FLASK CONFIGURATION
# ============================================================================
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here-change-in-production

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Local Development (SQLite)
# DATABASE_URL=sqlite:///devalert.db

# Production (Render PostgreSQL) - Render provides this automatically
# Example: postgresql://user:password@host:5432/database
# DATABASE_URL=postgres://devalert_user:password@dpg-xyz.render.com/devalert_db

# Local PostgreSQL
# DATABASE_URL=postgresql://localhost/devalert

# Local MySQL
# DATABASE_URL=mysql+pymysql://root:password@localhost/devalert

# ============================================================================
# EMAIL CONFIGURATION (Account Management Features)
# ============================================================================
# Required for: Email verification, Password reset
# Gmail SMTP (Development)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password-here
MAIL_FROM_EMAIL=noreply@devalert.com

# Production Email Services (Alternative options):
# SendGrid SMTP:
# MAIL_SERVER=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key

# Mailgun SMTP:
# MAIL_SERVER=smtp.mailgun.org
# MAIL_PORT=587
# MAIL_USERNAME=postmaster@your-domain.mailgun.org
# MAIL_PASSWORD=your-mailgun-smtp-password

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================
# Local Development
FRONTEND_URL=http://localhost:5173

# Production (Update with your actual domain)
# FRONTEND_URL=https://devalert.onrender.com

# ============================================================================
# OAUTH CONFIGURATION
# ============================================================================
# Google OAuth
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID and configure:
# - Authorized JavaScript origins: http://localhost:5000, https://your-domain.com
# - Authorized redirect URIs: http://localhost:5000/api/auth/oauth/google/callback
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# Get credentials from: https://github.com/settings/developers
# Create OAuth App and configure:
# - Homepage URL: http://localhost:5000 or https://your-domain.com
# - Authorization callback URL: http://localhost:5000/api/auth/oauth/github/callback
GITHUB_OAUTH_CLIENT_ID=your-github-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-github-client-secret

# ============================================================================
# GOOGLE CUSTOM SEARCH API (Optional - for AI event scanning)
# ============================================================================
# Get API key from: https://console.cloud.google.com/apis/credentials
# Create Custom Search Engine at: https://programmablesearchengine.google.com/
# Note: If not configured, scanner will use fallback sample data
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# Optional: Gemini AI API Key (for enhanced scanning)
GEMINI_API_KEY=
